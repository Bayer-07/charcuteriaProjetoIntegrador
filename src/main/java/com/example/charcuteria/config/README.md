### 🛠 `config/`
Centraliza as configurações de infraestrutura do Spring Framework.
* **`multitenancy/`**: Contém a lógica de isolamento de dados por cliente (Tenant). É aqui que o sistema decide qual Schema ou Banco acessar baseado no contexto da requisição.