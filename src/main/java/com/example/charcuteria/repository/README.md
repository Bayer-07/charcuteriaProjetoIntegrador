### 📦 `repository/`
Interface de comunicação com o banco de dados utilizando Spring Data JPA. Abstrai o SQL e gerencia as operações de CRUD (Create, Read, Update, Delete) de forma otimizada.
* note: se o professor quiser que seja em SQL vai ser por SQL nao Spring Data JPA